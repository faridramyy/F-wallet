# The deployed API base URL, printed by "sam deploy" as ApiUrl.
# This value ends up in the public JavaScript bundle, which is fine.
# It is a URL, not a secret. The API is protected by the login.

VITE_API_URL=https://abc123.execute-api.ca-central-1.amazonaws.com
